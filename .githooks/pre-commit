#!/bin/sh
# Blocks any commit containing a credential.
#
# Enabled by `git config core.hooksPath .githooks`, which `npm install` does
# automatically via the "prepare" script. To bypass in a genuine emergency:
#   git commit --no-verify
# — but understand that you are then committing unscanned content.

node scripts/scan-secrets.mjs || exit 1
